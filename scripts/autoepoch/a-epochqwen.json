{
    "default": {
        "train_batch_size": 1,
        "max_train_epochs": 100,
        "save_every_n_epochs": 20
    },
    "size_ranges": [
        {
            "min": 1,
            "max": 20,
            "config": {
                "max_train_epochs": 65,
                "save_every_n_epochs": 15
            }
        },
        {
            "min": 21,
            "max": 40,
            "config": {
                "max_train_epochs": 75,
                "save_every_n_epochs": 25
            }
        },
        {
            "min": 41,
            "max": 60,
            "config": {
                "max_train_epochs": 55,
                "save_every_n_epochs": 25
            }
        }
    ]
}